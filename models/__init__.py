""" Since the Restaurant model is to be created before the booking, so we are controlling the class initialization"""
from models.resturants import Restaurant
from models.customer import Customer
from models.bookings import Bookings
